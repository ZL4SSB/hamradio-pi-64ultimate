# ROIP Dashboards

EuroNode is preconfigured at `http://dvmega-euronode.local/`.

Add Pi-Star, WPSD, OpenWebRX, KiwiSDR or any other local web dashboard from the page. Entries are stored in `config/dashboards.json`.
