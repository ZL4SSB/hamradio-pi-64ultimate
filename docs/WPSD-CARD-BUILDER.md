# WPSD SD Card Builder

This helper prepares an SD card for a **separate Raspberry Pi fitted with an MMDVM board**.

It opens the official WPSD download page, validates the selected compressed image, displays detected storage devices, and then launches Raspberry Pi Imager.

It never automatically selects or writes to a drive.
