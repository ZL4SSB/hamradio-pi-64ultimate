# Preferences

Preferences are stored in `config/settings.json`.

Saved values include:

- theme name
- splash-screen setting
- automatic hardware scan
- automatic update check
- default CAT port
- default audio device
- preferred SDR
- update channel
- backup-before-update setting
- HamClock URL

The installer and public updater preserve `config/settings.json` where possible.
