# GPredict

External satellite tracking program. Set station coordinates, update TLE data and
check its timestamp before relying on passes. Rotator and radio control require
separately configured interfaces. Preview HRPU satellite geometry is not a live
position. GPredict remains a separate GPL application.
